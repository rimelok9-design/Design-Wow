{
  "buildCommand": "pnpm --filter @workspace/devforge run build",
  "outputDirectory": "artifacts/devforge/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
